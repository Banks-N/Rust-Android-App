//Automatically generated by rifgen
use crate::*;
use jni_sys::*;
foreign_class!(
	class Entry {
		self_type Entry;
		constructor Entry::new(name : String , weight : i64 , reps : i64 , sets : i64 , notes : String , date : String)->Entry;
		fn Entry::to_string(& self)->String; alias toString;
	}
);
foreign_class!(
	class Exercise {
		self_type Exercise;
		constructor Exercise::new(name : String , initial_date : String)->Exercise;
		fn Exercise::add_entry(& mut self , entry : Entry); alias addEntry;
		fn Exercise::get_log_string(& self)->String; alias getLogString;
	}
);
foreign_class!(
	class History {
		self_type History;
		constructor History::new(start_date : String)->History;
		fn History::add_exercise(& mut self , exercise : Exercise); alias addExercise;
		fn History::get_log_string(& self)->String; alias getLogString;
	}
);
foreign_class!(
	class RustLog {
		fn RustLog::initialise_logging(); alias initialiseLogging;
	}
);
